package dev.thomazz.lucid.accessor.server.play;

import dev.thomazz.lucid.accessor.PacketAccessor;
import dev.thomazz.lucid.packet.PacketType;

/**
* Accessor class generated by script for packet type {@link PacketType.Play.Server#CLOSE_WINDOW}
* Some types in getters and setters may not be supported properly yet.
*/
public final class CloseWindow extends PacketAccessor {
    public CloseWindow(Object handle) {
        super(PacketType.Play.Server.CLOSE_WINDOW, handle);
    }

    public CloseWindow() {
        super(PacketType.Play.Server.CLOSE_WINDOW);
    }

    public int getContainerId() {
        return this.get(0);
    }

    public void setContainerId(int value) {
        this.set(0, value);
    }
}
